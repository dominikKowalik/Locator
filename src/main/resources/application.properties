security.basic.enabled=false

logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=ERROR

spring.datasource.url=jdbc:mysql://localhost:3306/locatordatabase?autoReconnect=true&useSSL=false
spring.datasource.password=domo1234
spring.datasource.username=dominikKowalik

#enable logging
spring.jpa.show-sql=true


#Using the hibernate configuration ddl-auto =
#  update the database schema will be automatically created (and updated),
# creating tables and columns, accordingly to java entities found in the project.



# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1








